{% extends 'base.html.twig' %}

{% block title %}Mensaje{% endblock %}

{% block body %}
    <h1>Mensaje</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mensaje.id }}</td>
            </tr>
            <tr>
                <th>Asunto</th>
                <td>{{ mensaje.asunto }}</td>
            </tr>
            <tr>
                <th>Mensaje</th>
                <td>{{ mensaje.mensaje }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mensaje_index') }}">back to list</a>

    <a href="{{ path('app_mensaje_edit', {'id': mensaje.id}) }}">edit</a>

    {{ include('mensaje/_delete_form.html.twig') }}
{% endblock %}
